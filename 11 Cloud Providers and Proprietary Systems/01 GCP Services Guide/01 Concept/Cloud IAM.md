# Cloud IAM

> <- Back to [[GCP Security and Identity]]

Resource-level permissions with roles (basic, predefined, custom) and service accounts. Policy hierarchy: organization -> folder -> project -> resource. Policies are inherited downward. Equivalent to AWS IAM.

---

#gcp #security #iam
