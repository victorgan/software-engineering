# Persistent Disk

> <- Back to [[GCP Storage]]

Block storage for Compute Engine and GKE. Types: pd-ssd (SSD), pd-balanced (balanced performance/cost), pd-standard (HDD). Snapshots for backup. Regional persistent disks for high availability. Equivalent to AWS EBS.

---

#gcp #storage #persistent-disk #block-storage
