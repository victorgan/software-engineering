# Cloud Load Balancing

> <- Back to [[GCP Networking]]

Global L7 (HTTP/S) and regional L4 (TCP/UDP) load balancing, plus internal load balancing. Auto-scaling backends, SSL termination, URL maps for routing. A single global anycast IP for worldwide traffic. Equivalent to AWS ALB + NLB combined.

---

#gcp #networking #load-balancing
