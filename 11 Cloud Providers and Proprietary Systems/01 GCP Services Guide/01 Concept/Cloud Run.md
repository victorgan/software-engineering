# Cloud Run

> <- Back to [[GCP Compute]]

Serverless containers that auto-scale to zero. Any language/framework in a container. Built on Knative. The simplest way to deploy containerized applications on GCP. Equivalent to AWS Fargate + App Runner.

---

#gcp #compute #cloud-run #serverless #containers
