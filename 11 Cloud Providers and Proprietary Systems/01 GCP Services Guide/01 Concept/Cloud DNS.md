# Cloud DNS

> <- Back to [[GCP Networking]]

Managed DNS with DNSSEC support. 100% SLA uptime. Low latency DNS resolution using Google's global network. Equivalent to AWS Route 53.

---

#gcp #networking #dns
