# MemoryDB

> <- Back to [[AWS Databases]]

Redis-compatible in-memory database with multi-AZ durability. Combines the speed of Redis with the reliability of a durable database, eliminating the need for a separate persistence layer.

---

#aws #databases #memorydb #redis #durable
