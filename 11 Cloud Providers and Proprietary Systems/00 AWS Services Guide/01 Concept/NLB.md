# NLB

> <- Back to [[AWS Networking]]

Network Load Balancer. Layer 4 (TCP/UDP) load balancing with ultra-low latency, static IPs, and millions of requests per second. Used for non-HTTP workloads and extreme performance requirements.

## GCP Equivalent

Cloud Load Balancing (TCP/UDP)

---

#aws #networking #nlb #load-balancer #l4
