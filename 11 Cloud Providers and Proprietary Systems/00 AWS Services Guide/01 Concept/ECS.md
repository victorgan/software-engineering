# ECS

> <- Back to [[AWS Compute]]

Elastic Container Service. AWS-native Docker container orchestration. Defines tasks (container definitions) and services (desired count, load balancing). Can run on EC2 instances or Fargate (serverless).

## GCP Equivalent

[[Cloud Run]] (serverless) / [[GKE]] (orchestration)

---

#aws #compute #ecs #containers
