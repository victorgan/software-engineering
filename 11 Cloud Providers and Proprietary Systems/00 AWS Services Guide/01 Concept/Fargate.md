# Fargate

> <- Back to [[AWS Compute]]

Serverless compute for containers (ECS/EKS). No instance management -- AWS handles the underlying infrastructure. Pay per vCPU and memory per second. Simplifies container operations at the cost of less control.

## GCP Equivalent

[[Cloud Run]]

---

#aws #compute #fargate #serverless #containers
