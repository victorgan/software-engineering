# Dead Code Elimination

> <- Back to [[Optimization]]

Removing code that has no effect on the program's output. Dead code includes unreachable statements (after unconditional returns), unused variable assignments, and computations whose results are never consumed.

#language-implementation #optimization #dead-code
