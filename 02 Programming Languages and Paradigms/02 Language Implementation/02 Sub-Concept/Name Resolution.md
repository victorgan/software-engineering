# Name Resolution

> <- Back to [[Semantic Analysis]]

Connecting each use of a name (variable, function, type) to its declaration. Name resolution handles scoping rules, imports, overloading, and qualified names, ensuring that every identifier refers to exactly one definition.

#language-implementation #semantic #name-resolution
