# CUDA and OpenCL

> <- Back to [[GPU Computing]]

CUDA is NVIDIA's proprietary GPU programming platform; OpenCL is an open standard for heterogeneous parallel programming (GPUs, CPUs, FPGAs). Both enable launching thousands of parallel threads on GPU hardware for data-parallel computation.

#concurrency #data-parallelism #gpu #cuda #opencl
