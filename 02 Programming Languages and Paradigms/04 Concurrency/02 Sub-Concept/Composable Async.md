# Composable Async

> <- Back to [[Futures & Promises]]

Futures can be composed using operations like `thenApply` (map), `thenCompose` (flatMap), `allOf` (join all), and `anyOf` (first to complete). This enables building complex asynchronous workflows declaratively without nested callbacks.

#concurrency #models #futures #composable
