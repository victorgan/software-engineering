# Filter

> <- Back to [[Key Operations]]

Selects elements from a collection that satisfy a predicate function, producing a new collection containing only the matching elements. Filter preserves the order and type of elements while reducing the collection size.

#programming-paradigms #functional #filter
