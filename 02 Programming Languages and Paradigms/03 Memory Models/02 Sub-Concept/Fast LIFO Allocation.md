# Fast LIFO Allocation

> <- Back to [[Stack Memory]]

Stack allocation is just a pointer adjustment (increment to allocate, decrement to free), making it extremely fast compared to heap allocation. The LIFO discipline ensures automatic cleanup when functions return.

#memory-models #stack #lifo
