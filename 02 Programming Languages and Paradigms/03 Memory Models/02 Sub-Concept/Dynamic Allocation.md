# Dynamic Allocation

> <- Back to [[Heap Memory]]

Memory allocated at runtime with sizes determined during execution. The heap allocator must find suitable free blocks, track allocated regions, and handle deallocation, making it slower than stack allocation but essential for dynamic data structures.

#memory-models #heap #dynamic
