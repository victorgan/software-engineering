# Pages and Page Tables

> <- Back to [[Virtual Memory]]

Virtual memory is divided into fixed-size pages (typically 4KB). Page tables map virtual page numbers to physical frame numbers. Multi-level page tables reduce memory overhead for sparse address spaces while maintaining efficient translation.

#memory-models #virtual-memory #pages #page-tables
