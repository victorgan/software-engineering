# Retrieval Methods

> <- Back to [[Retrieval-Augmented Generation]]

Approaches for finding relevant documents. Dense retrieval (embedding similarity), sparse retrieval (BM25, keyword-based), and hybrid (combine both for best results). Re-ranking with cross-encoders improves precision.

#nlp #rag #retrieval
