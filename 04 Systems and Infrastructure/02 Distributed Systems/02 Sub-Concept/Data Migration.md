# Data Migration

> <- Back to [[Rebalancing]]

The process of physically moving data from one node to another during rebalancing. Must handle large data volumes without degrading performance. Techniques include streaming replication, bulk transfer with rate limiting, and incremental migration (move data in small batches over time).

#distributed-systems #sharding
