# Convergence Guarantee

> <- Back to [[Eventual Consistency]]

The promise that if no new writes occur, all replicas will eventually reach the same state. "Eventually" is unbounded — there is no guaranteed time limit. In practice, convergence usually happens within seconds to minutes depending on replication lag and network conditions.

#distributed-systems #consistency
