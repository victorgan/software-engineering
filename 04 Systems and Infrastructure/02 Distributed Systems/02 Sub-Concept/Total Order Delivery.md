# Total Order Delivery

> <- Back to [[Zab]]

All nodes receive and process messages in exactly the same order. This is stronger than causal ordering (which only orders causally related messages). Total order ensures that all replicas are identical at every point, simplifying correctness reasoning. The key property that Zab provides.

#distributed-systems #consensus
