# MX and TXT Records

> <- Back to [[Record Types]]

MX (Mail Exchange) records specify the mail servers for a domain with priorities — lower values have higher priority. TXT records store arbitrary text, commonly used for email authentication (SPF, DKIM, DMARC), domain ownership verification (Google, Let's Encrypt), and other metadata.

#networking #dns
