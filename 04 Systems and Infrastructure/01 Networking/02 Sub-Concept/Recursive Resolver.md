# Recursive Resolver

> <- Back to [[Resolution Process]]

The DNS server (usually provided by your ISP or a public resolver like 8.8.8.8 or 1.1.1.1) that receives queries from clients and recursively resolves them by querying the DNS hierarchy. Maintains a cache of previous lookups to speed up subsequent queries.

#networking #dns
