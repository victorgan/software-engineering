# 5xx Server Error Codes

> <- Back to [[Status Codes]]

500 Internal Server Error (generic server failure), 502 Bad Gateway (upstream server returned invalid response), 503 Service Unavailable (server overloaded or in maintenance), 504 Gateway Timeout (upstream server did not respond in time). 5xx errors are generally safe to retry with backoff.

#networking #http
