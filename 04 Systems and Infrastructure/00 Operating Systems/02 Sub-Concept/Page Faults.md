# Page Faults

> <- Back to [[Paging]]

Occurs when a process accesses a virtual page not currently in physical memory. Minor page faults resolve by mapping an already-loaded page; major page faults require loading the page from disk (very expensive, milliseconds). Excessive page faults cause thrashing.

#operating-systems #memory #paging
