# Pipes

> <- Back to [[Inter-Process Communication (IPC)]]

Unidirectional byte streams connecting the output of one process to the input of another. Anonymous pipes connect related processes (parent-child), while named pipes (FIFOs) allow communication between unrelated processes via a filesystem path.

#operating-systems #ipc
