# Sockets

> <- Back to [[Inter-Process Communication (IPC)]]

Bidirectional communication endpoints that work both locally (Unix domain sockets) and across networks (TCP/UDP sockets). The most versatile IPC mechanism, forming the basis of all network communication. Contrast with pipes which are local and unidirectional.

#operating-systems #ipc #networking
