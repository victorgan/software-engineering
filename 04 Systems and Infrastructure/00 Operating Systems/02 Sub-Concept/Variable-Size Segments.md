# Variable-Size Segments

> <- Back to [[Segmentation]]

Unlike fixed-size pages, segments can be any size, matching the logical structure of the program (code segment, data segment, stack segment). This natural mapping is intuitive but leads to external fragmentation as segments are allocated and freed.

#operating-systems #memory #segmentation
