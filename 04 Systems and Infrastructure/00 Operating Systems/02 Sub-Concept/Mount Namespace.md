# Mount Namespace

> <- Back to [[Namespaces]]

Isolates the set of filesystem mount points so that processes in different namespaces can have different views of the filesystem hierarchy. This enables containers to have their own root filesystem while sharing the host kernel.

#operating-systems #linux #containers
