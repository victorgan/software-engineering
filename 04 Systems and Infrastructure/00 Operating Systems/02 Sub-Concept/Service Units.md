# Service Units

> <- Back to [[systemd]]

Configuration files (*.service) that define how systemd manages a service: the executable to run, dependencies, restart policy, environment variables, and resource limits. Other unit types include timer (cron replacement), socket, mount, and target units.

#operating-systems #linux
