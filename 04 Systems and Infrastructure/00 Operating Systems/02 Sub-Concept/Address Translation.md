# Address Translation

> <- Back to [[Virtual Memory]]

The process of converting virtual addresses used by programs into physical addresses in RAM. The Memory Management Unit (MMU) hardware performs this translation using page tables, enabling each process to have its own isolated address space.

#operating-systems #memory #virtual-memory
