# Terraform Modules

> <- Back to [[Terraform]]

Reusable, composable packages of Terraform configuration. Modules accept input variables, create resources, and output values. The Terraform Registry hosts community modules for common patterns (VPC, EKS cluster, RDS database). Organizations build internal module libraries for standardized, compliant infrastructure.

#cloud #iac #terraform
