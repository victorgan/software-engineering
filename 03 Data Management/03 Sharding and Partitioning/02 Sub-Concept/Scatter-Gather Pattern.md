# Scatter-Gather Pattern

> <- Back to [[Cross-Shard Queries]]

A query pattern where the coordinator sends the query to all shards (scatter), collects results (gather), and merges them. Required when the query does not include the shard key.

#sharding #cross-shard #scatter-gather
