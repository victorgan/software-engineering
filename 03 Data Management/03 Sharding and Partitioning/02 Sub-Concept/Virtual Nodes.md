# Virtual Nodes

> <- Back to [[Consistent Hashing]]

Each physical node is mapped to multiple positions on the hash ring. Improves balance by distributing a node's responsibility across many segments rather than one contiguous range.

#sharding #consistent-hashing #vnodes
