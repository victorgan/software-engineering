# Query Alignment

> <- Back to [[Choosing a Shard Key]]

Most queries should include the shard key to avoid scatter-gather across all shards. The shard key should align with the dominant access pattern.

---

#sharding #shard-key #query-alignment
