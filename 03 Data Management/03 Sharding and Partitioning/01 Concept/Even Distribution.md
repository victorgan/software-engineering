# Even Distribution

> <- Back to [[Choosing a Shard Key]]

Avoid keys that create hotspots. For example, using timestamp as a shard key means all writes hit the latest shard. Good shard keys distribute both data and traffic evenly.

---

#sharding #shard-key #distribution
