# Partitioning Alternative

> <- Back to [[Sharding vs Alternatives]]

If data is large but queries are naturally scoped (by time, tenant), table partitioning within one instance may suffice without distributing data across multiple instances.

---

#sharding #alternatives #partitioning
