# Row-Level Locks

> <- Back to [[Locking]]

Locks acquired on individual rows, allowing maximum concurrency by only blocking access to the specific rows being modified.

#relational-databases #transactions #locking
