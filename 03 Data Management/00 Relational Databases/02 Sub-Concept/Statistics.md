# Statistics

> <- Back to [[Query Planner]]

Table and column statistics (row counts, value distributions, null fractions) that the query planner uses to estimate costs. Maintained by ANALYZE in Postgres and auto-updated by autovacuum.

#relational-databases #query-optimization #query-planner
