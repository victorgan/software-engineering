# No Transitive Dependencies

> <- Back to [[3NF]]

A 3NF requirement that no non-key attribute should depend on another non-key attribute. Every non-key attribute must depend directly on the primary key.

#relational-databases #normalization #3nf
