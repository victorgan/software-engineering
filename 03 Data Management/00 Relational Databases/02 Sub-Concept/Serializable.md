# Serializable

> <- Back to [[Isolation Levels]]

The highest isolation level. Transactions execute as if they were running one after another in serial order. Prevents all concurrency anomalies but with the highest performance cost.

#relational-databases #transactions #isolation
