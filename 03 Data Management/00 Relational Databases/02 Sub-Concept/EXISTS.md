# EXISTS

> <- Back to [[Subqueries]]

SQL operator that tests for the existence of rows returned by a subquery. Returns true if the subquery returns at least one row.

#relational-databases #sql #subqueries
