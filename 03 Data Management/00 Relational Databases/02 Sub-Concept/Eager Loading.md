# Eager Loading

> <- Back to [[N+1 Query Problem]]

Fetching related data upfront in a single query (using JOINs or subqueries) rather than lazily loading it one item at a time, preventing N+1 queries.

#relational-databases #query-optimization #n-plus-1
