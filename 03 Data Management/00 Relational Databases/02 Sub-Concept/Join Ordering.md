# Join Ordering

> <- Back to [[Common Optimizations]]

The query planner determines the optimal order to join tables, which can dramatically affect performance by reducing intermediate result set sizes.

#relational-databases #query-optimization
