# Time-Series for Temporal Data

> <- Back to [[Data Model]]

When data is primarily timestamped (metrics, IoT, financial), time-series databases (InfluxDB, TimescaleDB) provide optimized storage, compression, and time-range queries.

#database-selection #data-model
