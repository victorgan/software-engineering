# Relational for Structured Data

> <- Back to [[Data Model]]

When data has well-defined relationships, requires joins, and needs transactional guarantees, relational databases (Postgres, MySQL) are the natural fit.

#database-selection #data-model
