# Rich Data Structures

> <- Back to [[Redis]]

Beyond simple key-value, Redis supports lists, sets, sorted sets, hashes, streams, bitmaps, HyperLogLogs, and geospatial indexes as native data types.

#nosql #key-value #redis
