# Cypher Query Language

> <- Back to [[Neo4j]]

Neo4j's declarative graph query language using ASCII-art pattern matching syntax (e.g., (a)-[:KNOWS]->(b)) to express graph traversals.

#nosql #graph #neo4j #cypher
