# Collections and Documents

> <- Back to [[MongoDB]]

MongoDB's core data model. Documents (BSON records) are organized into collections (analogous to tables). No fixed schema is enforced at the collection level.

#nosql #document-stores #mongodb
