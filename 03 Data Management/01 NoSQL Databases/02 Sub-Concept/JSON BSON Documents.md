# JSON/BSON Documents

> <- Back to [[Document Store Concept]]

The storage format for document databases. JSON is human-readable; BSON (Binary JSON) is MongoDB's binary-encoded format that adds data types and is more efficient for storage and traversal.

#nosql #document-stores
