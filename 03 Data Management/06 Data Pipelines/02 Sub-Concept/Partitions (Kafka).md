# Partitions (Kafka)

> <- Back to [[Apache Kafka]]

Topics are split into partitions for parallel processing. Each partition is an ordered, immutable sequence of records. Partition count determines maximum consumer parallelism.

#data-pipelines #streaming #kafka #partitions
