# Short-Lived Tokens

> <- Back to [[OIDC for Cloud Auth]]

Credentials that expire after minutes rather than months or years. Short-lived tokens limit the window of exploitation if compromised. OIDC-based CI/CD authentication generates tokens valid only for the duration of a single pipeline run.

#supply-chain-security #ci-cd #tokens
