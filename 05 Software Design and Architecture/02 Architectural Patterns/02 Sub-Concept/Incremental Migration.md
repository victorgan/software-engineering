# Incremental Migration

> <- Back to [[Strangler Fig]]

Migrating a system piece by piece rather than all at once (big bang). Each piece is extracted, tested, and deployed independently while the old system continues running. Reduces risk by limiting the scope of each change.

#property #migration #incremental
