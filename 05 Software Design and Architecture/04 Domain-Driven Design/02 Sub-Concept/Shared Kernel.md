# Shared Kernel

> <- Back to [[Context Mapping]]

A small, explicitly shared subset of the domain model that two bounded contexts agree to maintain together. Changes to the shared kernel require agreement from both teams. Keep it small to minimize coordination overhead.

#property #ddd #shared-kernel
