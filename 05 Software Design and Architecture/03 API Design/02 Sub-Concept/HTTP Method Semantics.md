# HTTP Method Semantics

> <- Back to [[Resource-Oriented Design]]

GET retrieves, POST creates, PUT replaces, PATCH partially updates, DELETE removes. GET and HEAD are safe (no side effects). GET, PUT, DELETE are idempotent. POST is neither safe nor idempotent.

#property #rest #http-methods
