# Fluent Interface

> <- Back to [[Builder]]

A method chaining API where each method returns the builder object itself, enabling readable construction code like `builder.setName("x").setAge(5).build()`. Makes complex object construction read like a domain-specific language.

#property #builder #fluent-interface
