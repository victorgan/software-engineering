# Wrapping Delegation

> <- Back to [[Decorator]]

The decorator wraps the original object and forwards calls to it, adding behavior before or after delegation. The wrapper and the wrapped object share the same interface, making them interchangeable from the client's perspective.

#property #decorator #wrapping
