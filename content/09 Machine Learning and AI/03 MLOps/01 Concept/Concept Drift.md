# Concept Drift

> <- Back to [[Model Monitoring]]

When the relationship between inputs and outputs changes over time. The "rules" the model learned are no longer correct. Example: user behavior changes during a pandemic. More insidious than data drift because the input distribution may look the same.

## Related

- [[Data Drift]] (input distribution changes)
- [[Retraining Triggers]] (concept drift triggers retraining)

---

#mlops #monitoring #concept-drift
