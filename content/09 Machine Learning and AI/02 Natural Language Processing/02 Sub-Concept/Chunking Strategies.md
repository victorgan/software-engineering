# Chunking Strategies

> <- Back to [[Retrieval-Augmented Generation]]

How to split documents into chunks for embedding and retrieval. Fixed-size (token count), semantic (split at natural boundaries), recursive (split hierarchically by separators). Chunk size affects retrieval quality: too small loses context, too large dilutes relevance.

#nlp #rag #chunking
