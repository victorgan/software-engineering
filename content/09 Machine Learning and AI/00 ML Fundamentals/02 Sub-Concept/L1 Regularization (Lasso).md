# L1 Regularization (Lasso)

> <- Back to [[Linear Models]]

Adds the sum of absolute values of weights as a penalty term. Drives some weights exactly to zero, performing automatic feature selection. Produces sparse models.

#ml #regularization #lasso #l1
