# Margin Maximization

> <- Back to [[Support Vector Machines]]

SVM finds the decision boundary that maximizes the distance (margin) between the closest points of different classes (support vectors). Larger margins typically lead to better generalization.

#ml #svm #margin
