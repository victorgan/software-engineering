# Trigger-Based CDC

> <- Back to [[Change Data Capture (CDC)]]

Use database triggers to capture changes when rows are inserted, updated, or deleted. Higher overhead on the source database but simpler to implement.

---

#data-pipelines #cdc #trigger-based
