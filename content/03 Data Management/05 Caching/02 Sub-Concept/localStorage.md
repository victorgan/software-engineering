# localStorage

> <- Back to [[Browser Caching]]

Browser API for storing key-value pairs persistently on the client. Synchronous access, 5-10MB limit, string values only. Useful for caching user preferences and small datasets.

#caching #browser #localstorage
