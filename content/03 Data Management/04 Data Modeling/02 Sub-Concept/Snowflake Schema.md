# Snowflake Schema

> <- Back to [[Schema Design Patterns]]

A normalized variation of the star schema where dimension tables are further broken into sub-dimensions. More normalized, less redundancy, but more complex joins.

#data-modeling #relational #schema-patterns #snowflake-schema
