# Vertical Scaling First

> <- Back to [[Sharding vs Alternatives]]

Modern hardware (64+ cores, TBs of RAM, NVMe SSDs) handles more than most assume. Postgres on a large instance handles millions of rows. Don't shard until you must.

---

#sharding #alternatives #vertical-scaling
