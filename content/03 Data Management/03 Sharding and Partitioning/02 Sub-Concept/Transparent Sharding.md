# Transparent Sharding

> <- Back to [[Vitess]]

The application sends queries as if talking to a single database. The sharding middleware transparently routes queries to the correct shard without application code changes.

#sharding #tools #vitess
