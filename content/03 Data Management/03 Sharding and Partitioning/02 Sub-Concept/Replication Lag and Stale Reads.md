# Replication Lag and Stale Reads

> <- Back to [[Read Replicas]]

The delay between a write on the primary and its appearance on a replica. During this window, reads from the replica return stale data. Critical to account for in application logic.

#sharding #replication #lag
