# Partition Pruning

> <- Back to [[Partitioning (Single-Node)]]

Query planner optimization that skips irrelevant partitions during query execution. A major performance win for queries on large partitioned tables.

---

#relational-databases #partitioning #optimization
