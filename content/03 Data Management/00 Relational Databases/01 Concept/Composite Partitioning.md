# Composite Partitioning

> <- Back to [[Partitioning (Single-Node)]]

Combine multiple partitioning methods (e.g., range by year, then hash by user_id within each year) for more granular data distribution.

---

#relational-databases #partitioning #composite
