# Non-Correlated Subqueries

> <- Back to [[Subqueries]]

Subqueries that are independent of the outer query and execute only once. The result is then used by the outer query.

#relational-databases #sql #subqueries
