# Write Skew

> <- Back to [[Concurrency Phenomena]]

An anomaly where two transactions each read a value, make decisions based on those reads, and write results that violate a constraint that neither transaction individually violated. Only prevented by serializable isolation.

#relational-databases #transactions #concurrency-phenomena
