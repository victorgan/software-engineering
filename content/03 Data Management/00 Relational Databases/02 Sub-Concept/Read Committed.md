# Read Committed

> <- Back to [[Isolation Levels]]

Transactions only see data committed before each statement executes. Prevents dirty reads but allows non-repeatable reads. Default in PostgreSQL.

#relational-databases #transactions #isolation
