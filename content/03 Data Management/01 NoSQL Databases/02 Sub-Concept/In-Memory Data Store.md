# In-Memory Data Store

> <- Back to [[Redis]]

Redis stores all data in RAM for sub-millisecond access times. Optional persistence (RDB snapshots, AOF logs) provides durability while maintaining in-memory speed.

#nosql #key-value #redis
