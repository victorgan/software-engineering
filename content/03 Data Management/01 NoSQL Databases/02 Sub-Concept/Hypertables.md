# Hypertables

> <- Back to [[TimescaleDB]]

TimescaleDB's abstraction that automatically partitions time-series data into chunks by time interval while presenting a single virtual table to the user.

#nosql #time-series #timescaledb
