# Partition Key + Sort Key

> <- Back to [[DynamoDB]]

DynamoDB's primary key model. The partition key determines which partition stores the item. The optional sort key enables range queries within a partition and allows multiple items per partition key.

#nosql #key-value #dynamodb
