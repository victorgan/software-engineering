# Constant Folding

> <- Back to [[Optimization]]

Evaluating constant expressions at compile time rather than runtime. For example, `2 * 3 + 1` is replaced by `7` during compilation, eliminating unnecessary runtime computation.

#language-implementation #optimization #constant-folding
