# Sum Types (Tagged Unions)

> <- Back to [[Algebraic Data Types (Type Systems)]]

Types where a value can be exactly one of several named variants, each potentially carrying different data. The tag identifies which variant is active. Examples include Rust enums, Haskell data types, and TypeScript discriminated unions. Enable exhaustive pattern matching.

#type-systems #adt #sum-types #tagged-unions
