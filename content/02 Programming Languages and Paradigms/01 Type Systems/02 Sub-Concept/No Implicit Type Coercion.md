# No Implicit Type Coercion

> <- Back to [[Strong Typing]]

The language does not automatically convert between types. Operations like adding a string and an integer produce an error rather than silently converting one to the other. Explicit conversions must be written by the programmer, making type interactions clear and predictable.

#type-systems #strong #coercion
