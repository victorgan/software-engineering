# Multiple Readers Single Writer

> <- Back to [[Readers-Writer Pattern]]

A concurrency pattern where data can be read by many threads simultaneously but written by only one at a time, with no concurrent readers during writes. This maximizes throughput for read-heavy workloads while ensuring write consistency.

#concurrency #patterns #readers-writer
