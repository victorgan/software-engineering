# Transactions on Shared Memory

> <- Back to [[Software Transactional Memory (STM)]]

STM groups reads and writes to shared memory into transactions that execute atomically and in isolation. If no conflicts occur, the transaction commits. This provides the illusion of serial execution without manual lock management.

#concurrency #models #stm #transactions
