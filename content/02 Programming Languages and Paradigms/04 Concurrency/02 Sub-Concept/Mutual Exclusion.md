# Mutual Exclusion

> <- Back to [[Mutexes - Locks]]

The property that only one thread can execute a critical section at any given time. Mutexes enforce mutual exclusion by blocking other threads that attempt to acquire an already-held lock until the holder releases it.

#concurrency #synchronization #mutual-exclusion
