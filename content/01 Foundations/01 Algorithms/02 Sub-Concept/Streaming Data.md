# Streaming Data

> <- Back to [[Reservoir Sampling]]

Data arriving continuously and potentially infinitely, where it is impractical to store the entire dataset. Reservoir sampling handles streaming data by maintaining a fixed-size sample that is updated as new elements arrive, using O(k) space for a sample of size k.

#algorithms #randomized #streaming #reservoir-sampling
