# Likelihood

> <- Back to [[Bayesian Statistics]]

The probability of the observed data given a particular parameter value: L(theta) = P(data | theta). Not a probability distribution over theta. Maximum likelihood estimation (MLE) finds the parameter that maximizes the likelihood. In Bayesian statistics, likelihood connects prior to posterior via Bayes' theorem.

#mathematics-for-cs #statistics #bayesian #likelihood
