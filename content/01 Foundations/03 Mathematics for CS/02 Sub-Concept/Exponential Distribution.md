# Exponential Distribution

> <- Back to [[Distributions]]

Models the time between events in a Poisson process. The only continuous memoryless distribution. P(X > x) = e^(-lambda * x). Used in modeling server response times, component lifetimes, and inter-arrival times in queuing theory.

#mathematics-for-cs #probability #distributions #exponential
