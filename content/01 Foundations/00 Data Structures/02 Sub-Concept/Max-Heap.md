# Max-Heap

> ← Back to [[Heaps]]

Heap where the parent is always greater than or equal to its children. The root is the maximum element. Used in heap sort and max-priority queues.

#property #heaps
