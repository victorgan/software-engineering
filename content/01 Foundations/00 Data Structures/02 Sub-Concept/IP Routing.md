# IP Routing

> ← Back to [[Tries]]

Longest prefix matching in network routing uses a bitwise trie. Each bit of an IP address determines left/right traversal. The deepest matching prefix determines the route.

#property #tries #networking
