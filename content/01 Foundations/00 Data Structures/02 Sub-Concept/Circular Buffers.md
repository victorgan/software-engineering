# Circular Buffers

> ← Back to [[Queues]]

Fixed-size buffer that wraps around using modular arithmetic. When full, new data overwrites the oldest. Used in I/O buffering, audio processing, and producer-consumer patterns. Also called a ring buffer.

#property #queues #buffers
