# Pointer-Based Traversal

> ← Back to [[Linked Lists]]

Elements are accessed by following pointers from one node to the next, not by index. This means O(n) access to the k-th element but O(1) insertion/deletion at any known position — the opposite tradeoff from [[Arrays]].

#property #linked-lists
