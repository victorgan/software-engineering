# Deques

> ← Back to [[Queues]]

Double-ended queue — supports insertion and removal at both ends in O(1). Generalizes both stacks and queues. Python's collections.deque, Java's ArrayDeque.

#property #queues
