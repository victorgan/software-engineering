# Collision Resolution

> ← Back to [[Hash Tables]]

When two keys hash to the same index, a collision resolution strategy is needed. The two main approaches are [[Chaining]] and [[Open Addressing]]. Load factor (n/capacity) determines when to resize.

#property #hashing
