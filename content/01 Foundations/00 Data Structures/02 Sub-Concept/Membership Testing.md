# Membership Testing

> ← Back to [[Hash Sets]]

Check whether an element exists in the set in O(1) average time. The primary operation of a set. Alternative to scanning a list in O(n).

#property #hashing
