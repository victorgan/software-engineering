# Expression Evaluation

> ← Back to [[Stacks]]

Stacks are used to evaluate arithmetic expressions. Shunting-yard algorithm converts infix to postfix notation; a stack then evaluates the postfix expression left to right. Also used in parsing (matching parentheses, brackets).

#property #stacks
