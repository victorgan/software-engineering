# Branch Refs

> <- Back to [[Refs]]

Simple files containing the SHA-1 hash of the latest commit on the branch. When a new commit is made, the branch ref is updated to point to the new commit. Branches are lightweight — just 41-byte files.

#property #git #branch-refs
