# Rule Configuration

> <- Back to [[Linters]]

Linters are configured through config files (.eslintrc, pyproject.toml, .rubocop.yml) where rules can be enabled, disabled, or set to warning/error severity. Teams customize rules to match their coding standards and gradually increase strictness.

#property #linting #rule-configuration
