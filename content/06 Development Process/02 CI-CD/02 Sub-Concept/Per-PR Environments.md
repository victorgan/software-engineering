# Per-PR Environments

> <- Back to [[Ephemeral Environments]]

Each pull request automatically gets its own isolated environment (preview deployment). Reviewers can test changes in a realistic setting without affecting shared environments. Environments are automatically destroyed when the PR is merged or closed.

#property #cicd #ephemeral #per-pr
