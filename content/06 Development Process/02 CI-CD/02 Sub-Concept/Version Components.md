# Version Components

> <- Back to [[Semantic Versioning]]

MAJOR (breaking changes that require consumer updates), MINOR (new features that are backward-compatible), PATCH (backward-compatible bug fixes). Pre-release versions use suffixes like -alpha, -beta, -rc.1.

#property #cicd #semver #version-components
