# Gherkin Syntax

> <- Back to [[Behavior-Driven Development]]

A structured natural language format: Given (preconditions), When (action), Then (expected outcome). Gherkin scenarios are readable by non-technical stakeholders and executable by tools like Cucumber. Bridges the gap between requirements and tests.

#property #testing #gherkin
