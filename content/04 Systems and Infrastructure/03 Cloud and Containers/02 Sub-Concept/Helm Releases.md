# Helm Releases

> <- Back to [[Helm]]

A deployed instance of a chart with a specific set of values. Releases are named and versioned, supporting upgrades, rollbacks, and history tracking. Multiple releases of the same chart can coexist in a cluster (e.g., separate Redis instances for different services).

#cloud #containers #kubernetes
