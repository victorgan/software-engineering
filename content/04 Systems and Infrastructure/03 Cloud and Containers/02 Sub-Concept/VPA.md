# VPA

> <- Back to [[K8s Scaling]]

Vertical Pod Autoscaler — automatically adjusts CPU and memory resource requests and limits for pods based on historical usage. Helps right-size pods that are over- or under-provisioned. Can operate in recommendation mode (suggest changes) or auto mode (apply changes, requires pod restart).

#cloud #containers #kubernetes #scaling
