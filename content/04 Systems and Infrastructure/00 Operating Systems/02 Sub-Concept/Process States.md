# Process States

> <- Back to [[Process Lifecycle]]

The five main states of a process: Created (new), Ready (waiting for CPU), Running (executing on CPU), Waiting/Blocked (waiting for I/O or event), and Terminated (finished execution). The OS scheduler manages transitions between these states.

#operating-systems #processes #lifecycle
