# External Fragmentation

> <- Back to [[Segmentation]]

Free memory becomes scattered in small, non-contiguous chunks as variable-size segments are allocated and freed. Even if total free memory is sufficient, no single contiguous block may be large enough. This is the primary disadvantage of segmentation compared to paging.

#operating-systems #memory #segmentation
