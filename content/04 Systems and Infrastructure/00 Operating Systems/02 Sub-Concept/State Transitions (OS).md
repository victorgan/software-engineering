# State Transitions (OS)

> <- Back to [[Process Lifecycle]]

Processes move between states based on OS scheduling decisions and events. Key transitions: Ready to Running (scheduled), Running to Waiting (I/O request), Waiting to Ready (I/O complete), Running to Ready (preempted), Running to Terminated (exit).

#operating-systems #processes #lifecycle
