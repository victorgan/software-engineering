# Partition Tradeoff

> <- Back to [[PACELC Theorem]]

During a network partition, the system must choose between availability (continue serving possibly stale data) and consistency (reject requests until the partition heals). This is the "PAC" part of PACELC and is equivalent to the CAP theorem's tradeoff.

#distributed-systems #fundamentals
