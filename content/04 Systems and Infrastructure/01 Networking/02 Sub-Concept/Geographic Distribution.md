# Geographic Distribution

> <- Back to [[Edge Caching]]

CDN edge servers are deployed across many geographic locations (Points of Presence). Users are routed to the nearest PoP via anycast DNS or BGP anycast. Major CDNs have hundreds of PoPs globally, ensuring most users are within a few milliseconds of an edge server.

#networking #cdn
