# Mutual TLS

> <- Back to [[Service Mesh]]

mTLS where both client and server authenticate each other using certificates. The service mesh automatically manages certificate issuance, rotation, and verification. Provides encryption in transit and service identity verification without application code changes. Zero-trust networking principle.

#networking #service-mesh #security
