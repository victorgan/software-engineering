# Full-Duplex Communication

> <- Back to [[WebSockets]]

Both client and server can send messages independently at any time without waiting for a response. Unlike HTTP's request-response pattern, either side can initiate communication. This enables real-time updates without polling or long-polling workarounds.

#networking #websockets #real-time
