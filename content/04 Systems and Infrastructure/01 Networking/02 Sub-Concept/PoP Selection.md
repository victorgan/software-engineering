# PoP Selection

> <- Back to [[Edge Caching]]

How users are routed to the nearest CDN edge server. Anycast routing (same IP announced from multiple locations, BGP routes to nearest) is most common. DNS-based routing uses geo-aware DNS to return the nearest edge server's IP. Some CDNs use a combination of both approaches.

#networking #cdn
