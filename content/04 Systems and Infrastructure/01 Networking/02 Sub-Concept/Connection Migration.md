# Connection Migration

> <- Back to [[HTTP 3]]

QUIC connections are identified by a connection ID rather than the (IP, port) tuple used by TCP. When a client changes networks (e.g., Wi-Fi to cellular), the connection continues seamlessly without re-establishing. Critical for mobile users who frequently switch networks.

#networking #http #quic
