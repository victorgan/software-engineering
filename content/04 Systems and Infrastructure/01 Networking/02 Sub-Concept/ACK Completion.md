# ACK Completion

> <- Back to [[Three-Way Handshake]]

The client's final acknowledgment that completes the three-way handshake. After this, both sides are in the ESTABLISHED state and data transfer can begin. The ACK can carry data (TCP Fast Open allows data in the SYN itself to save a round trip).

#networking #tcp
