# Header Inspection

> <- Back to [[Layer 7 Load Balancing]]

L7 load balancers can read and modify HTTP headers. Use cases include adding X-Forwarded-For (client IP), X-Request-ID (request tracing), routing based on Authorization headers, and injecting custom headers for backend processing. Also enables request/response transformation.

#networking #load-balancing
