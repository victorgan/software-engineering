# Level Hierarchy

> <- Back to [[Log Levels]]

The ordered severity of log levels: DEBUG < INFO < WARN < ERROR < FATAL. Setting the log level to WARN means only WARN, ERROR, and FATAL messages are emitted, reducing noise while preserving critical information.

#observability #logging #log-levels
