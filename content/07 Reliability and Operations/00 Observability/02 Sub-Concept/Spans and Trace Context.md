# Spans and Trace Context

> <- Back to [[Traces]]

A span represents a unit of work within a trace, containing a name, start/end timestamps, attributes, and status. Trace context (trace ID + span ID) is propagated across service boundaries to correlate spans into a complete trace.

#observability #traces #spans
