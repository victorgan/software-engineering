# Schema Consistency

> <- Back to [[Structured Logging]]

Maintaining consistent field names and types across all services and log entries. Inconsistent schemas (e.g., "userId" vs "user_id" vs "user-id") make cross-service querying difficult. Schema registries or shared logging libraries enforce consistency.

#observability #logging #schema
