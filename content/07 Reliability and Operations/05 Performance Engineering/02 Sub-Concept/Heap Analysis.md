# Heap Analysis

> <- Back to [[Memory Profiling]]

Examining the contents of the memory heap to identify which objects are consuming memory, what is retaining them, and whether they can be garbage collected. Heap dumps and heap analysis tools reveal memory leaks and excessive allocation.

#performance #profiling #heap
