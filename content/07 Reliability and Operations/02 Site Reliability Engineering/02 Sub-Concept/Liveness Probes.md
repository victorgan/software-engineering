# Liveness Probes

> <- Back to [[Health Checks]]

Health checks that determine if a process is alive and functioning. If a liveness probe fails, the container orchestrator restarts the process. Liveness probes detect deadlocks, infinite loops, and other unrecoverable states. They should be simple and fast.

#sre #health-checks #liveness
