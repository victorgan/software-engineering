# Upstream Signaling

> <- Back to [[Backpressure Propagation]]

Mechanisms for downstream services to communicate their capacity to upstream callers. HTTP 429 (Too Many Requests), gRPC flow control, and reactive streams backpressure allow services to propagate load information through the system, preventing cascading overload.

#traffic-management #backpressure #signaling
