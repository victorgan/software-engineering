# Incremental Backups

> <- Back to [[Backup Strategies]]

Backup of only the data that has changed since the last backup (full or incremental). Faster to create and smaller to store, but restoring requires the full backup plus all subsequent incrementals in order.

#disaster-recovery #backup #incremental
