# Google Zanzibar Model

> <- Back to [[ReBAC]]

Google's global authorization system that models permissions as relationships between entities in a graph. Zanzibar evaluates authorization by traversing relationship tuples (user, relation, object). Open-source implementations include SpiceDB, OpenFGA, and Ory Keto.

#authorization #rebac #zanzibar
