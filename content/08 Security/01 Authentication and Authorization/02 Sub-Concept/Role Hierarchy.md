# Role Hierarchy

> <- Back to [[RBAC]]

A structure where higher roles inherit permissions from lower roles. For example, Admin inherits all Editor permissions, which inherits all Viewer permissions. Role hierarchies simplify permission management but must be designed carefully to avoid unintended privilege accumulation.

#authorization #rbac #role-hierarchy
