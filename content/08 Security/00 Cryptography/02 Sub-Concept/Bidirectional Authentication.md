# Bidirectional Authentication

> <- Back to [[mTLS]]

In mutual TLS, both the client and server present certificates to authenticate each other. This prevents unauthorized services from communicating within internal networks. Both parties verify the other's certificate against their trust store before establishing the connection.

#cryptography #mtls #authentication
