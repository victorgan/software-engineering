# Context Boundary

> <- Back to [[Bounded Contexts]]

The explicit line that separates one model from another. Within the boundary, terms are precise and the model is consistent. Communication across boundaries requires explicit translation through context mapping patterns.

#property #ddd #context-boundary
