# Consistency Boundary

> <- Back to [[Aggregates]]

The aggregate defines the boundary of strong consistency. Within an aggregate, invariants are enforced transactionally. Between aggregates, eventual consistency is accepted. This boundary determines the granularity of transactions.

#property #ddd #consistency-boundary
