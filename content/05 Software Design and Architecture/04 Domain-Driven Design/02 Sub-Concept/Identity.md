# Identity

> <- Back to [[Entities]]

The defining characteristic of an entity. Two entities with different identities are different objects even if all their attributes are identical. Identity can be natural (email, SSN) or surrogate (UUID, auto-increment ID).

#property #ddd #identity
