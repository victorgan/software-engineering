# Structural Equality

> <- Back to [[Value Objects]]

Two value objects are equal if all their attributes are equal, regardless of whether they are the same instance in memory. This contrasts with entities, which are equal only if they have the same identity.

#property #ddd #structural-equality
