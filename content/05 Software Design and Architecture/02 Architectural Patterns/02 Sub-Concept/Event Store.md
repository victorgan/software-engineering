# Event Store

> <- Back to [[Event Sourcing]]

An append-only database optimized for storing events. Events are never modified or deleted, only appended. The event store serves as the system of record, with current state derived by replaying events from the beginning or from a snapshot.

#property #event-sourcing #event-store
