# Separate Read Write Models

> <- Back to [[CQRS]]

The write model (command side) is optimized for processing commands and enforcing business rules. The read model (query side) is optimized for serving queries with denormalized views. Each model can use different data stores and schemas.

#property #cqrs #read-write-models
