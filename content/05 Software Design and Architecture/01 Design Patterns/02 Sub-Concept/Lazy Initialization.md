# Lazy Initialization

> <- Back to [[Singleton]]

Delaying the creation of an object until the first time it is needed. Common in singleton implementations to avoid unnecessary resource allocation at startup. Must handle thread safety in concurrent environments.

#property #singleton #lazy-initialization
