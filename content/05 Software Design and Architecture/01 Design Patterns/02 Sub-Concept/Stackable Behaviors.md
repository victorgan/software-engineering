# Stackable Behaviors

> <- Back to [[Decorator]]

Multiple decorators can be stacked on top of each other, each adding its own behavior. The order of decoration matters — behaviors are applied in the order decorators are wrapped, creating a layered composition.

#property #decorator #stackable
