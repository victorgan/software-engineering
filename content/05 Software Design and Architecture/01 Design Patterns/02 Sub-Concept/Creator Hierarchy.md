# Creator Hierarchy

> <- Back to [[Factory Method]]

The parallel class hierarchy where each creator subclass is responsible for instantiating a corresponding product subclass. The creator declares the factory method; concrete creators override it to produce specific products.

#property #factory #creator-hierarchy
