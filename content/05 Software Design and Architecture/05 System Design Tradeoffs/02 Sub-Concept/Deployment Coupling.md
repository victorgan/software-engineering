# Deployment Coupling

> <- Back to [[Coupling vs Autonomy]]

When changes to one component require deploying other components simultaneously. Monoliths have high deployment coupling (everything deploys together). Microservices reduce deployment coupling but require careful interface versioning.

#property #tradeoffs #deployment-coupling
