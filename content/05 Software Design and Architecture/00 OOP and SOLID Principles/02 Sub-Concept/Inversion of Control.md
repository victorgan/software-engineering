# Inversion of Control

> <- Back to [[Dependency Inversion Principle]]

A principle where the flow of control is inverted: instead of the caller controlling what happens, a framework or container calls your code. Dependency injection is the most common form of IoC.

#property #dip #inversion-of-control
