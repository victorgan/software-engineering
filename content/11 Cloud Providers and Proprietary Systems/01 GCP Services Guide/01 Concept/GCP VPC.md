# GCP VPC

> <- Back to [[GCP Networking]]

Virtual Private Cloud. Global resource (unlike AWS which is regional). Subnets, firewall rules, shared VPC across projects. Auto-mode (automatic subnets in all regions) or custom-mode.

---

#gcp #networking #vpc
