# Cloud Storage

> <- Back to [[GCP Storage]]

Object storage (GCS). Buckets with storage classes: Standard, Nearline (monthly access), Coldline (quarterly access), Archive (yearly access). Versioning, lifecycle management, and strong consistency. Equivalent to AWS S3.

---

#gcp #storage #gcs #object-storage
