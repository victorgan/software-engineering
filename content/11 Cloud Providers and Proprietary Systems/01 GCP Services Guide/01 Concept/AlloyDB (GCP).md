# AlloyDB (GCP)

> <- Back to [[GCP Databases]]

Google-built Postgres-compatible database. Columnar engine for analytics mixed with transactional workloads. Up to 4x faster than standard PostgreSQL for transactions, 100x faster for analytics. Equivalent to AWS Aurora.

---

#gcp #databases #alloydb #relational
