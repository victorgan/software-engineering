# IAM (AWS)

> <- Back to [[AWS Security and Identity]]

Identity and Access Management. Users, groups, roles, and JSON policies. The principle of least privilege: grant only the permissions needed. IAM is global (not region-specific). Roles enable cross-service and cross-account access.

## GCP Equivalent

Cloud IAM

---

#aws #security #iam
