# Secrets Manager

> <- Back to [[AWS Security and Identity]]

Store, rotate, and manage secrets (database passwords, API keys, tokens). Automatic rotation for RDS, Redshift, and DocumentDB credentials. Accessed via API, CLI, or SDK.

## GCP Equivalent

Secret Manager

---

#aws #security #secrets-manager
