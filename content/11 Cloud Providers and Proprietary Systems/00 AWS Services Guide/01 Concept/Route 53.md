# Route 53

> <- Back to [[AWS Networking]]

DNS service with health checks and advanced routing policies: latency-based (route to fastest region), geolocation, failover (active-passive), weighted (traffic splitting). Also serves as domain registrar.

## GCP Equivalent

[[Cloud DNS]]

---

#aws #networking #route53 #dns
