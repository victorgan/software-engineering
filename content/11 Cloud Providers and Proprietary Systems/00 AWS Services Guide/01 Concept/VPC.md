# VPC

> <- Back to [[AWS Networking]]

Virtual Private Cloud. Isolated network with subnets (public/private), route tables, internet gateways, and NAT gateways. The foundation of AWS networking -- all resources run inside a VPC.

## GCP Equivalent

GCP VPC

---

#aws #networking #vpc
