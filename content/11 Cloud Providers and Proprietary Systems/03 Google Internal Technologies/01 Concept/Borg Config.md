# Borg Config

> <- Back to [[Google Configuration and Deployment]]

Configuration language for Borg jobs (BCL — Borg Config Language). Evolved to GCL (Generic Config Language), then to Jsonnet (open-sourced by Google). Jsonnet influenced the CUE configuration language. Declarative specification of workload requirements, resources, and scheduling constraints.

---

#google-internal #deployment #borg-config #configuration
