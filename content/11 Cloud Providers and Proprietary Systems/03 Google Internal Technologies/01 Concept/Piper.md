# Piper

> <- Back to [[Google Build and Development]]

Google's monorepo — a single code repository containing almost all of Google's code (billions of lines). Managed via CitC (Clients in the Cloud), which provides virtual file system workspace snapshots. One of the largest single repositories in the world.

---

#google-internal #build #piper #monorepo
