# Open Addressing

> ← Back to [[Hash Tables]]

All entries stored directly in the array. On collision, probe for the next available slot. Methods: linear probing, quadratic probing, double hashing. Better cache performance than chaining but more sensitive to load factor. Python dict uses open addressing.

#property #hashing
