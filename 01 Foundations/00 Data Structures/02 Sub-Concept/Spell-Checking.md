# Spell-Checking

> ← Back to [[Tries]]

Store a dictionary as a trie. Checking if a word exists is O(m) where m is word length. Combined with edit distance, enables "did you mean?" suggestions.

#property #tries
