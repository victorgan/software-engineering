# Edge List

> ← Back to [[Graph Representations]]

Simple list of (source, destination, weight) tuples. Space O(E). Simplest representation. Used in Kruskal's algorithm (sort edges by weight). Poor for neighbor lookups.

#property #graphs
