# Deduplication

> ← Back to [[Hash Sets]]

Remove duplicate elements from a collection by inserting all elements into a set. Only unique elements are retained. O(n) time and space.

#property #hashing
