# Unimodal Functions

> <- Back to [[Ternary Search]]

Functions that have exactly one local maximum (or minimum) in a given interval. Ternary search exploits the unimodal property to find the extremum in O(log n) time by eliminating one-third of the search space at each step.

#algorithms #searching #ternary-search #unimodal
