# Locally Optimal Choices

> <- Back to [[Core Concept (Greedy)]]

The principle of always selecting the option that looks best at the current step, without reconsidering previous choices. This greedy choice property, combined with optimal substructure, guarantees that local decisions lead to a globally optimal solution for qualifying problems.

#algorithms #greedy #locally-optimal
