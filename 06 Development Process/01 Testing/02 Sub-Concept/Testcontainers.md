# Testcontainers

> <- Back to [[Database Tests]]

A library that provides lightweight, throwaway instances of databases, message brokers, and other services as Docker containers. Tests run against real dependencies, ensuring high fidelity without polluting shared environments.

#property #testing #testcontainers
