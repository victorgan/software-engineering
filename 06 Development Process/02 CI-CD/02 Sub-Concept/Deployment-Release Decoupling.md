# Deployment-Release Decoupling

> <- Back to [[Feature Flags]]

Separating the act of deploying code to production from the act of releasing features to users. Code can be deployed (in production, running) but not released (not visible to users) until the feature flag is turned on.

#property #cicd #feature-flags #decoupling
