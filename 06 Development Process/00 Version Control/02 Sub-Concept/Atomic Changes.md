# Atomic Changes

> <- Back to [[Monorepo]]

In a monorepo, a single commit can modify multiple projects, libraries, or services simultaneously. This ensures that cross-cutting changes (like API contract changes) are always consistent and never in a partially-migrated state.

#property #git #atomic-changes
