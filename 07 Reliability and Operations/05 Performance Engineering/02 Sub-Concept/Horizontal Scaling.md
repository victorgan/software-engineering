# Horizontal Scaling

> <- Back to [[Auto-Scaling]]

Adding more instances of a service to handle increased load. Horizontal scaling (scaling out) is preferred for stateless services because it provides linear capacity increase and can scale indefinitely, unlike vertical scaling which has hardware limits.

#performance #auto-scaling #horizontal
