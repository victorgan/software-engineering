# User-Centric Measurement

> <- Back to [[SLI (Service Level Indicator)]]

SLIs should measure what users experience, not what the system reports internally. For example, measure latency at the load balancer (what users see) rather than at the application server (which excludes network and queuing time).

#sre #sli #user-centric
