# Circuit States (Closed, Open, Half-Open)

> <- Back to [[Circuit Breakers]]

The three states of a circuit breaker: Closed (normal operation, requests pass through), Open (circuit tripped, requests fail fast without calling the downstream), Half-Open (periodic test requests allowed to check if the downstream has recovered).

#sre #circuit-breaker #states
